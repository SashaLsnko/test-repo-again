version: 2.1

jobs:
  sleep:
    docker:
      - image: cimg/base:current
    steps:
      - run:
          name: Sleep for 1 minute
          command: sleep 60

workflows:
  sleep-workflow:
    jobs:
      - sleep:
          serial-group: << pipeline.project.slug >>/sleep
